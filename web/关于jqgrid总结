	1、今天做项目时，遇到了一个问题，就是代码嵌套问题
  出现页面：HTML，
  原因：例如使用了jqgrid，它对发送请求进行了一次异步ajax封装，具有beforesend，loadcomplete等功能。
  用户进入页面，点击搜索，jqgrid会根据搜索栏条件，发送分页、排序等功能。
  2、我的onclick事件1 触发发送异步请求的功能，onclick事件2为隐藏域赋值并增加搜索条件。
  3、onclick事件2点击后触发事件1。
  4、这样会导致一个问题，服务端只能接受有隐藏域值的数值，而无法去掉。
  5、解决方法：直接分两次发送ajax事件，即事件1和事件2分别发ajax，这样就解决了混排的效果。
