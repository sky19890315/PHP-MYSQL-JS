目标：实现在数组遍历中，某条语句只执行一次。
<?php
$arr = [[0=>a],[1=>b],[2=>c],[3=>d],[4=>e]];
$lock = true;
foreach ($arr as $key => $value){
  if($lock){
  array_push([5=>f]);
  }
  $lock = false;
}
分析：php的数组遍历是将一个数组复制出来，我们的所有操作都是基于这个
复制出的数组进行的。遍历的值是一条条独立的变量，所有操作都是基于变量实现的。
在遍历开始前，进行的初始化设置。在遍历执行一次后，锁失败，不再执行入栈请求。
